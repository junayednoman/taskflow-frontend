import TasksContainer from "./_components/TaskContainer";

export const metadata = {
  title: "Tasks",
};

const TasksPage = () => {
  return (
    <main>
      <TasksContainer />
    </main>
  );
};

export default TasksPage;
